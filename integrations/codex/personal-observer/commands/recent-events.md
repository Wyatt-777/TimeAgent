# Recent Events

Use `observer_get_recent_events` with a bounded limit of 10 unless the user
requests another limit. Summarize the newest normalized events with timestamp,
type, source, priority, and relevant data. Do not modify the project.
