# Investigate Latest

Use the `investigate-event` skill for the latest available events and pending
alerts. Compare the observed context with read-only Git status, distinguish
facts from inference, and state when the available evidence is insufficient.
Do not run commands, tests, edits, commits, pushes, or automatic investigation.
