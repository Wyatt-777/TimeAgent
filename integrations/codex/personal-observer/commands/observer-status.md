# Observer Status

Use the `observer-status` skill to report the current Local PC Agent status.
Include event count, pending alert count, active session count, and database
schema version. Keep the response concise and read-only.
